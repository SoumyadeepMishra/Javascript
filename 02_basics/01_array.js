// array

const myArr = [1,2,3]
console.log(myArr[0])

const numbers = new Array(4,5,6,7,7,8)
console.log(numbers)

myArr.push(6)
myArr.pop() // pop the last value of array

myArr.unshift(9) // inserts the element into the first in array
myArr.shift() // shifts back the array

const newArr = myArr.join() // add the all the elements of the array into a string


////// slice splice
// splice manipulates the original array itself 
